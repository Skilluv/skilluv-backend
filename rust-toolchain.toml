# Pin the Rust toolchain for reproducible builds across dev + CI + prod.
# Bump this together with any language feature bumps in edition or Cargo.toml.
[toolchain]
channel = "1.97.1"
components = ["clippy", "rustfmt"]
profile = "minimal"
